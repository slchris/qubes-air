# harness-backlog — Qubes Air（repo 级实践学习）

> canonical repo memory: `<PROJECT_ROOT>/.kixpower/memory/repo/harness-backlog.md`。
> 由 L4 Hill Climbing 在 Sprint 收尾前维护；每条记录必须满足
> `scripts/validate-memory-backlog.ps1` 的字段契约（本机无 pwsh，按等价检查执行）：
> `id / type / problem / improvement / source / evidence / eval` + `status`（candidate|validated|archived）。
> `candidate` 需 `kind: origin`；`validated` 需 `kind: trial` + `result: pass`；`archived` 需 `archive_reason`。
>
> 状态机（TEAM_CONVENTIONS §实践学习状态机）：
> candidate（单次证据，非规则）→ scoped trial → validated / archived。
> 无 `status` 的历史条目按 candidate 读取，不得因一次历史成功晋升为规则。

## Active records

<!-- 暂无记录：本 Sprint 为该仓库首次 kixpower 导入（mode 0），
     Sprint 1 的 L4 之前没有可复用的实践证据写入。 -->

## Archived records

<!-- 无 -->
