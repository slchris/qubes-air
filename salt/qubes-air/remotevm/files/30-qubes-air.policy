# /etc/qubes/policy.d/30-qubes-air.policy
# =====================================================================
# Qubes Air 阶段2 —— RemoteVM + 零入站 SSH transport 的 qrexec policy
#
# 【单一来源】本文件是 Qubes Air qrexec policy 的唯一权威来源。
# 旧的散落副本 (dom0-scripts/init-qubes-air.sh 内联 heredoc、
# ansible/playbooks/setup-sys-remote.yaml 内联 policy、旧的 80-qubes-air.policy)
# 均已废弃, 见每个文件里的弃用说明。请只维护此文件。
#
# 【新格式列义】(Qubes R4.x 新 qrexec policy, 每行 5 列 + 可选参数):
#     SERVICE   ARGUMENT   SOURCE   DESTINATION   ACTION [参数...]
#   - SERVICE:     RPC 服务名。不允许 glob (不能写 qubes-air.*)。要匹配"任意服务"用单独一行 `*`。
#   - ARGUMENT:    `*` = 任意参数(含无参); `+ARG` = 精确匹配该参数。
#   - SOURCE:      调用发起方。可用具体 qube 名、`@tag:xxx`、`@type:xxx`、`*`。
#   - DESTINATION: 目标。可用具体名、`@default`、`@adminvm`、`@dispvm`、`@tag:xxx`、`*`。
#   - ACTION:      allow / deny / ask。
#                    ask 可带 default_target=<vm> (预选) 、target=<vm> (重定向)。
#                    allow/ask 可带 target=、user=。
#   - 匹配顺序: 自上而下, 第一条命中即生效 -> 具体规则在前, 兜底 deny 在最后。
#
# 【安全原则 (本链路强约束)】
#   1. 唯一可信授权决策点 = 本地 dom0 (即本文件)。远端 policy 只作纵深防御。
#   2. 所有"远端 -> 本地"调用、破坏性/高危操作一律 ask, 远端来源永不 allow。
#   3. Relay (sys-relay-*) 绝不允许直达 @adminvm (dom0 Admin API)。删除任何此类 allow。
#   4. 用 tag (@tag:remote-zone) 管理 RemoteVM 集合, 不用通配符 glob。
#
# 【RemoteVM 改写如何触发】
#   当本地 AppVM 对一个 RemoteVM 目标发起 qrexec 调用时, dom0 policy 引擎识别目标是
#   RemoteVM, 自动把请求改写成对其 relayvm 的一次调用:
#       transport_rpc + <RemoteVM名> + <原服务> + <原参数>
#   即 Relay 上实际收到的服务是 `qubesair.SSHProxy`。因此授权分两层:
#     (a) 允许本地 AppVM 把该服务发往 RemoteVM 目标 (下方 A 段);
#     (b) 允许改写后的 qubesair.SSHProxy 落到 Relay (下方 B 段)。
# =====================================================================


# ---------------------------------------------------------------------
# A 段: 本地 AppVM -> RemoteVM (正向调用, 面向具体细粒度服务)
#
# 只放行明确列出的细粒度服务。破坏性操作用 ask, 让用户在 dom0 弹窗确认。
# 目标用 @tag:remote-zone 覆盖所有 RemoteVM (由 create-remotevm.sh 打的 tag)。
# ---------------------------------------------------------------------

# 只读健康检查: 允许 (低风险)
qubesair.Ping           *   @tag:remote-work   @tag:remote-zone   allow

# 只读状态查询: 允许
qubesair.Status         *   @tag:remote-work   @tag:remote-zone   allow

# 远端命令/部署等有副作用的操作: ask (每次弹窗确认, 预选调用目标)
qubesair.Exec           *   @tag:remote-work   @tag:remote-zone   ask

# 文件推送到远端 (有副作用): ask
qubesair.Deploy         *   @tag:remote-work   @tag:remote-zone   ask

# 兜底: 其余到 RemoteVM 的任何服务一律拒绝
# (显式列出目标为 @tag:remote-zone 的兜底, 避免误放行未定义服务)
*                       *   @anyvm             @tag:remote-zone   deny


# ---------------------------------------------------------------------
# B 段: transport 改写后落到 Relay (qubesair.SSHProxy)
#
# 改写后 dom0 会以"原始 SOURCE (本地 AppVM)"为来源、Relay 为目标, 调用 transport_rpc。
# 仅允许被 @tag:remote-work 的本地 AppVM 使用 transport, 且目标必须是 @tag:relay。
# ---------------------------------------------------------------------

qubesair.SSHProxy       *   @tag:remote-work   @tag:relay         allow

# 其它任何来源想调用 transport 一律拒绝 (防止非授权 qube 借道 Relay)
qubesair.SSHProxy       *   @anyvm             @anyvm             deny


# ---------------------------------------------------------------------
# C 段: 反向调用 (远端 -> 本地) —— 零入站方案的回程
#
# 远端 qube 经 ssh -R 反向端口 + Relay 回环 sshd + qrexec-client-vm 调本地服务
# (如 Split-GPG / vault 取密)。这类调用在 dom0 看来 SOURCE 是发起 transport 的
# Relay (Relay 能在其名下 qube 间伪造来源, 但不能伪造未走它的 qube —— 见信任边界)。
#
# 强约束: 远端 -> 本地 一律 ask, 永不 allow。目标限定到具体本地服务 VM。
# ---------------------------------------------------------------------

# 远端请求本地 Split-GPG 签名/解密: ask, 预选 vault, 绝不 allow
qubes.Gpg               *   @tag:relay         vault              ask default_target=vault

# 远端请求读取本地 vault 里的 secret: ask
qubesair.VaultRead      *   @tag:relay         vault              ask default_target=vault

# 兜底: Relay 作为来源的其它一切调用一律拒绝 (尤其禁止到 dom0)
qubes.VMShell           *   @tag:relay         @anyvm             deny
*                       *   @tag:relay         @adminvm           deny
*                       *   @tag:relay         @anyvm             deny


# ---------------------------------------------------------------------
# D 段: 显式禁止 Relay / RemoteVM 触达 dom0 Admin API (纵深防御)
#
# 无论如何, 本链路里的任何 relay/remote 相关来源都不得直连 dom0。
# 放在 admin.* 默认策略之前, 明确拒绝。
# ---------------------------------------------------------------------

admin.vm.Start          *   @tag:relay         @adminvm           deny
admin.vm.Remove         *   @tag:relay         @adminvm           deny
*                       *   @tag:remote-zone   @adminvm           deny


# ---------------------------------------------------------------------
# E 段: vault-cloud 凭据隔离下发 (阶段3)
#
# vault-cloud 是无网络 AppVM (netvm=''), 打 tag=vault-cloud, 持有云 API 凭据 +
# relay SSH 私钥 + age/SOPS 私钥。红线: 私钥永不出 vault, 控制台(mgmt-air)不持有明文。
#
# 授权原则 (同本文件 C 段的 ask 原则): 一切"取凭据/用私钥"一律 ask (人工在 dom0 弹窗确认),
# 永不 allow。目标锁定到 @tag:vault-cloud。default_target 预选 vault-cloud 便于确认。
# ---------------------------------------------------------------------

# E1. 控制台按名取凭据 (云 API key / age 私钥等): mgmt-air -> vault-cloud, 每次 ask。
#     ARGUMENT 用 * (凭据名经 +ARG 传入, 服务端脚本再做白名单校验)。
#     若想按凭据名收紧, 可为每个名字写一行 +proxmox-token 等精确规则放在此行之前。
qubesair.GetCredential  *   mgmt-air           @tag:vault-cloud   ask default_target=vault-cloud

# E2. split-ssh: sys-relay 经 agent socket 用 vault 里的私钥签名 (拿不到私钥本身)。
#     每次调用 ask (可按需改为 allow, 但评审红线倾向 ask; 高频场景见 runbook 折衷)。
qubes.SshAgent          *   @tag:relay         @tag:vault-cloud   ask default_target=vault-cloud

# E3. split-gpg: 若 age/SOPS 之外还用 GPG, 允许 mgmt-air 请求 vault-cloud 做 GPG 操作, ask。
#     (阶段2 C 段已有 @tag:relay->vault 的 qubes.Gpg; 这里补 mgmt-air->vault-cloud 一条。)
qubes.Gpg               *   mgmt-air           @tag:vault-cloud   ask default_target=vault-cloud

# E4. 兜底: 到 vault-cloud 的其它任何服务/来源一律拒绝 (未定义服务不放行)。
#     尤其: vault-cloud 无网络, 也不该被当作跳板; 除上面三类外全部 deny。
*                       *   @anyvm             @tag:vault-cloud   deny

# E5. vault-cloud 作为来源: 它不该主动发起任何 qrexec (纯被动响应)。全部拒绝, 含 dom0。
#     显式 @adminvm 在前 (文档意图), 再兜底 @anyvm。
*                       *   @tag:vault-cloud   @adminvm           deny
*                       *   @tag:vault-cloud   @anyvm             deny


# =====================================================================
# 逐行列义速查 (供监工对照官方"新 qrexec policy"格式复查):
#
#   qubesair.Ping   *   @tag:remote-work   @tag:remote-zone   allow
#   └SERVICE        └ARG └SOURCE           └DESTINATION       └ACTION
#   读作: "来自打了 remote-work tag 的本地 qube、发往任意 RemoteVM(@tag:remote-zone)、
#          服务 qubesair.Ping、任意参数 -> 允许"。
#
#   qubes.Gpg  *  @tag:relay  vault  ask default_target=vault
#   读作: "来源是 Relay、目标 vault、服务 qubes.Gpg -> 弹窗询问, 对话框预选 vault"。
#
# 待真机确认:
#   [C1] RemoteVM 目标改写后, dom0 是否用 *原始本地 AppVM* 作为 SSHProxy 调用的 SOURCE
#        (本文件按此假设写 B 段); 若 R4.3 实现用 Relay 自身作为 SOURCE, B 段 SOURCE 改为对应值。
#   [C2] 反向调用 (C 段) 在 dom0 看到的 SOURCE 究竟是 @tag:relay 还是远端映射名。
#        官方 #9475 "source info" 若已生效, 可能能看到更细来源; 需实机 `qrexec` 日志确认。
#   [C3] tag 名 (remote-work / relay / remote-zone) 是本项目约定, 需与 salt/脚本打 tag 保持一致:
#          - 本地可发起远程调用的 AppVM 打 remote-work
#          - 本地 Relay AppVM 打 relay
#          - RemoteVM 打 remote-zone (由 create-remotevm.sh 自动打)
#
# 阶段3 新增 (vault-cloud, E 段) 待真机确认:
#   [E1] mgmt-air 是控制台(console) 所在 qube 的名字, 属本项目约定; 若你的控制台 qube 另有其名,
#        把 E 段 SOURCE 的 `mgmt-air` 改成实际名 (或改用 @tag: 标签)。
#   [E2] tag=vault-cloud 由 create-vault-cloud.sh 自动打; 若合并进已有 `vault`, 给 vault 也打此 tag,
#        或把 @tag:vault-cloud 改成具体 vault 名。
#   [E3] qubes.SshAgent 是社区 Split SSH 约定的服务名 (forum.qubes-os.org/t/split-ssh/19060);
#        官方核对该名与脚本内容; 高频 autossh 场景是否把 E2 由 ask 放宽为 allow 属策略权衡, 见 runbook。
#   [E4] 官方 qubes.Gpg (Split GPG) 服务名与 ask 语义; age/SOPS 若不用 GPG 可删 E3。
# =====================================================================
