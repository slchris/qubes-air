# Qubes Air Console - Frontend Environment Configuration
# =====================================================
# Copy this file to .env.local and modify as needed.
# Variables prefixed with VITE_ are exposed to the browser.

# ===================
# Development Server
# ===================

# Host to bind the dev server (use 0.0.0.0 for all interfaces)
VITE_DEV_HOST=127.0.0.1

# Port for the dev server
VITE_DEV_PORT=5173

# Backend API target URL (for proxying in dev mode)
VITE_API_TARGET=http://127.0.0.1:8080

# ===================
# TLS Configuration
# ===================

# Enable HTTPS for dev server
VITE_TLS_ENABLED=false

# Path to TLS certificate file
VITE_TLS_CERT=

# Path to TLS private key file
VITE_TLS_KEY=

# ===================
# API Configuration
# ===================

# Override API base URL (for production builds connecting to different backend)
# Leave empty to use same origin (recommended)
# Example: https://api.example.com/api/v1
VITE_API_BASE_URL=
